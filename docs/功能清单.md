# NetPeek 功能清单

## 屏 0：外观与背景（2026-09-02 新增，核心差异化功能）

背景图用户自定义，UI 样式随图自适应。两种自适应模式：

- 背景来源：默认深色 / 内置壁纸若干 / 自定义本地图片
- 自适应模式选择：标准自适应（默认）/ AI 自适应 / 关闭（手动固定主题）
- **标准自适应**：从图片提取主色，自动生成强调色、玻璃面板底色、文字明暗；提供微调（面板不透明度、背景模糊强度两个滑杆）；完全离线、即时生效
- **AI 自适应**：把图片缩略图发送给多模态模型，返回整套配色与焦点区域；首次使用需授权（明示“缩略图将上传至所选服务”），提供方可选；结果按图片缓存，生成失败或断网自动回退标准模式
- 焦点避让：自动检测画面主体位置，面板默认避开焦点区域；用户仍可手动拖动窗口
- 实时预览：换图、换模式、调滑杆即时预览，可一键还原默认

## 屏 1：主界面「实时」

- 顶栏：应用图标与名称；监控状态（监控中 / 已暂停 / 服务异常）；总下载速率、总上传速率、今日合计流量；搜索框输入即筛；深浅色切换；设置入口
- 实时速率总览：总上传 / 总下载速率随时间变化图，时间窗切换 10s / 1m / 5m，悬浮显示某一时刻的速率数值
- 应用榜单：横向 Top 4 应用卡片，每张显示应用图标、名称、实时下载 / 上传速率、占总量百分比
- 进程列表主表格，列：应用图标 | 应用名 | PID | 下载速率 | 上传速率 | 下载总量 | 上传总量
  - 点击列头排序
  - 搜索过滤（与顶栏搜索联动）
  - 视图切换：按应用聚合 / 按进程明细
  - 选中一行后右侧弹出详情窗格
- 右侧详情窗格（选中应用后显示）：
  - 应用图标、名称、完整路径、PID、会话时长
  - 该应用近 1 小时速率曲线
  - 该应用近 30 天每日用量柱状图
  - 「归因说明」入口，解释代理 / VPN / WSL 场景下流量的归因边界
- 底部状态栏：
  - ETW 丢事件提示（无丢失时显示「监控正常」）
  - 服务运行状态
  - 本次会话已采集时长
  - 归因覆盖率（如 96% 归因到应用 / 4% 系统未归因）

## 屏 2：迷你窗（托盘小窗）

- 置顶、可拖动
- 顶部：总下载 / 总上传实时速率
- 中部：Top 3 应用（图标、名称、速率），点击可定位到主界面对应行
- 底部：暂停 / 打开主界面 / 退出 三个按钮

## 屏 3：历史统计

- 日期范围选择：昨日 / 近 7 天 / 近 30 天 / 自定义
- 每日总量柱状图（上传 / 下载分开统计）
- 明细表：应用 | 下载合计 | 上传合计 | 占比 | 趋势箭头
- 导出 CSV

## 屏 4：设置

- 常规：登录时启动 UI、托盘常驻、速率单位（自动 / KB / MB / GB）
- 外观：跳转「屏 0：外观与背景」
- 采集：数据保留周期（默认 30 天）、是否记录系统未归因流量
- 主题：跟随系统 / 深色 / 浅色（背景图启用时此选项退化为文字明暗基准）
- 服务状态卡片：采集服务运行状态、ETW 事件丢失计数、重启服务按钮
- 关于：版本号、签名信息

## 屏 5：空态 / 异常态

- 无网络活动：主界面显示「当前没有检测到网络活动」
- 服务未启动：顶栏异常提示 + 中部「采集服务未运行」卡片 + 「启动服务」按钮
- ETW 丢事件：底部提示「事件丢失 N 条，显示数值可能不准」

## 托盘图标与右键菜单

- 托盘图标：可切换为实时速率模式（动态显示速率）或静态模式
- 右键菜单：打开主界面 / 打开迷你窗 / 暂停监控 / 开机自启 / 退出