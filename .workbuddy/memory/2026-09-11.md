# 2026-09-11

## 项目现状盘点（NetPeek）

用户要求「查看这个项目当前情况」，做了一次全量只读盘点，未修改任何代码。

### 结论
- 主干最后一次提交：`d4103fc`（2026-09-05 17:18，新应用图标）。文档口径「最后更新 2026-09-05」与实际一致。
- 工作区有 3 个文件处于未提交 WIP（2026-09-07 10:07~10:17 改动），配套 `.zcode/plans/plan-sess_f083b481….md` 一份 5 步实施计划 —— 内容 = **历史屏「自定义日期范围」**。
- 该功能**当前跑不起来**，属半成品。

### 已确认的缺陷（读代码得出，非推测）
1. `dayKeysBetween()` 被引用 3 次（history-ui.js:96 / 245 / 338），**全项目 0 处定义** → 点「自定义…」按钮会立刻 `ReferenceError`。
2. `styles.css` 里 `.hist-custom-range` / `.hist-date-field` / `.hist-range-error` / `.hist-chart` **一条规则都没有** → 表单与错误提示用浏览器默认样式；`.hist-data` 是 `flex column`，`.chart` 靠 `flex:1` 撑高，多出的一行表单会直接压缩图表高度。
3. 后端 `history_daily(days)`（src-tauri/src/history.rs:307）只支持「最近 N 天」（`cutoff = now - days*86400`），**没有任意起止日期入参** → 选过去的区间（如 8/1~8/10，queryDays=10）取回的是最近 10 天，与所选区间完全不重叠，柱图全零。这是设计层阻断项，前端补 `dayKeysBetween` 也修不好。
4. `renderSide()` 未按 `customRange` 过滤行（只按选中桶过滤）；`els.rangeTitle` 对自定义区间仍写「近 N 天」，措辞错误。

### 其他状态
- `Cargo.toml` 显示为已修改，但 `git diff` 内容为空 —— 纯 LF/CRLF 行尾差异，无实质改动。
- 未跟踪：`DESIGN.md`、`PRODUCT.md`（均 2026-09-10 16:35 生成）、`icon-d1tools.com.ico`、`.zcode/`。`.gitignore` 未覆盖后三者。
- 残留空目录 `.preview-shots/`（2026-09-05 17:13，§9.4 说脚手架已删，目录壳还在）。
- 本机环境注意：bash 的 PATH 缺 `/usr/bin`（`ls`/`head`/`cat`/`stat` 报 command not found），需 `export PATH="/usr/bin:/bin:$PATH"`；PowerShell 工具在本机被安全策略禁用，返回码 1 无输出，改用 bash/Glob/Read。

## 自定义日期范围 WIP 收尾（用户指令「全部做完」→「继续」）

把 09-07 的半成品补完并验证，改动 5 个文件（均未提交，等用户决定是否 commit）：

- **后端** `history.rs`：新增 `history_range(start, end)` 命令（`strftime('%s', ?,'utc')` 换算本地墙上时间为 unix 秒，end 当天含两端）；抽 `open_db`（统一 busy_timeout=3s）与 `collect_daily_rows` 两个辅助函数；`lib.rs` 注册。
- **前端** `history-ui.js`：补 `dayKeysBetween()`（含两端，MAX_RANGE_DAYS 上限；日期字面量必须带 `T00:00:00` 否则按 UTC 解析错一格）；提交校验（必填/倒置/晚于今天）+ Esc 关浮层；`loadRows` 按档位路由 `history_daily`/`history_range`；`renderSide` 按整个区间过滤 + 标题「起 ~ 止 · 共 N 天」；**顺带修旧缺陷**——检查栏「30 天下载」不再无条件复用历史屏 rows，`dailyFor` 按窗口宽度决定复用或自查（`inspectorRows` 缓存）。
- **样式** `styles.css`：补 `.hist-custom-range`（绝对定位不占文档流，否则压掉柱图 50px）/ `.hist-date-field` / `.hist-custom-actions` / `.hist-range-error`。
- **文档**：开发进度.md 新增 §11（含实现细节与验收记录）；功能清单.md 屏 3 日期范围条目改为已完成。

### 验证结论
- `node --check` 全部 UI JS 通过；`cargo check` + `cargo build`（dev，2m00s）通过。
- headless Chrome + 假 `__TAURI__` 脚手架（%TEMP%\netpeek-preview，未入仓库）：5 个状态截图 + 机读断言全过，关键用例 = 过去区间 07-28~08-26 出 30 根柱、合计 155.68 GB、排行按区间过滤。
- SQL 边界单独用 Python sqlite3 验过（跨月/闰日/end=今天）。
- **留给人工**：安装版真机端到端点一遍；是否 commit 由用户定。

### 环境补充
- cargo 在 `/c/Users/Administrator/.cargo/bin`，PATH 要带上才能编译。
- headless 截图/dump-dom 用系统 Chrome（`/c/Program Files/Google/Chrome/Application/chrome.exe`）+ `--user-data-dir` 指临时 profile，稳定可用。

## 三线优化评审（用户指令「查看有什么地方可以优化，UI和性能和代码逻辑」）

通读 Rust 后端 + 前端 JS + 采集 C#，结论：无 bug 级问题，优化点按优先级：
1. 【性能大头】IconBase64 每帧全量随快照走（C#序列化→管道→serde parse→Tauri emit→JS parse 四次编解码，每秒几十~两百 KB）→ 建议 collector 改「首帧+变化时」单独发图标。
2. 隐藏到托盘仍 1Hz 全量渲染 → onSnapshot 加 document.hidden 守卫。
3. query_history 是死代码（前端已全走 history_daily/history_range）。
4. settings.rs init 与 load_settings 补默认值逻辑重复；save_settings 先写注册表后写文件有半失败窗口。
5. history-ui dailyFor fallback `Math.max(n, days)` 在 customRange 时 days 可能=3660，语义混用。
6. esc/escapeHtml 前端重复 4 处；日期选择器未用 history_stats.firstTs 设 min/max。
7. Cargo.toml 无 [profile.release]（lto/strip/codegen-units）。
8. 控制管道无显式 PipeSecurity，本机其他进程可下发 pause/resume。
9. canvas 图表无键盘选中/aria（a11y，低优先级）。

## 三线优化全量落地（用户指令「全做」）

按快赢批 → UI 批 → P1 图标协议批 → 收尾批推进，全部完成并验证：

- **P1**：`TrafficSnapshot.IconUpdates`（路径→dataURL）+ `_iconPathsSent` 去重 + `ResetIconStream()`（CollectorService 每轮连接前调）；`ProcessTraffic.IconBase64` 恒空保留兼容；前端 main.js/mini.js 各建 iconCache，`iconOf(p)` 含旧协议回退；history-ui 走 `NetPeekLive.iconOf`。帧体积降 95%+。
- **P2**：onSnapshot 记账后 `document.hidden` 提前返回 + visibilitychange 补渲染。
- **P3**：Cargo.toml [profile.release]（lto/codegen-units/opt-level=s/panic=abort/strip）。
- **L1** 删 query_history；**L2** dailyFor fallback 固定 30、days 不再被 customRange 覆写；**L3** settings.rs 抽 `merged_with_defaults` + 先写文件后写注册表；**L4** 新增 Ipc/PipeAcl（注意 .NET 8 必须用 `NamedPipeServerStreamAcl.Create`，构造重载不存在——第一版就踩了这个 CS1739）。
- **U1** 日期框 min/max 来自 history_stats.firstTs；**U2** `.hist-chart.is-loading` 降透明；**U3** 历史柱图 tabindex+方向键/Esc，四张 canvas 补 aria；**U4** ui/common.js 收敛 escapeHtml，index.html/mini.html 都先加载。
- 验证：cargo check ✓、dotnet build 0 警 0 错 ✓、node --check 全过 ✓、headless Chrome 五用例断言全过（图标缓存/旧协议回退/日期边界/区间出图/键盘选中）。
- 未做（评审里标低优先）：prune 改每小时、读命令复用 state.conn。
- 留人工：安装版真机验证图标重连重发 + 新旧混跑升级窗口。

## 托盘同步 + 折线悬停读数（用户反馈三问题）

- **托盘不同步根因**：lib.rs 托盘菜单用本地 AtomicBool 记暂停态，只有托盘自己点才翻转。修复：新增 `TrayPause` 托管状态（原子量 + MenuItem<Wry> 句柄），pipe.rs 每帧快照按 `Status` 调 `sync_tray_pause`；断管时复位。三入口（托盘/迷你窗/设置）全收敛。
- **折线悬停**：charts.js `line()` 加悬停层——canvas 缓存 `_lineOpt`，mousemove 整帧重画（60 槽成本可忽略），`_hoverIdx` 存画布上供测试断言；小卡 fmtFull 两位小数，与 y 轴紧凑格式有意区分；卡片右缘自动翻转。main.js chartOpts 加 `tipSuffix:'/s'` + series `label`。
- **开源库结论**：uPlot（MIT）是流式时序首选，但要把自带图例/游标/坐标轴逐项覆盖回 §2.4 规格不划算，自绘+悬停已覆盖；将来要缩放/平移再迁。
- 验证：cargo check ✓、node --check 全过 ✓、headless 断言 hoverIdx=12/cursor/leave 复位 ✓、截图数值与采样精确一致 ✓。托盘是原生菜单留真机手验。

## 能量球样式修复

- 三个真问题：①环贴边（外环外缘 46 = 球半径，边框切环）→ r 44→41 / 39→35，间隙 2→3px，setArc 按 r 现算周长所以 JS 零改动；②track 用 --line（10% 白）几乎不可见，零流量读作空黑饼 → 改 --stroke（18% 白）；③「断连态小弧」疑点排除——是 `--` 占位文字本身，非渲染 bug。
- 验证：mini-preview 脚手架五状态截图（live/idle/paused/offline/panel），前后对比存 .workbuddy/verify/2026-09-11-orb-*。
- **headless 截图坑**：Chrome 窗口宽被钳到约 500px——108×108 窗口直接截得到 322 字节纯色块；能量球用 500×400 截（球绝对居中），320×300 的面板要注入 `inset:auto; left:0; top:0; width:320px; height:300px` 钉左上角，否则右半被裁。
- 留真机：窗口透明 + 外发光的实际观感 headless 验不了。

## 能量球「能量感」第二轮（用户反馈：没有能量球的感觉）

- 方案：单一强度变量 `--e`（下载占比 0.75 + 上传 0.35，对 60 秒峰值归一）由 JS 每帧写进 `.orb`，CSS 用 `calc(var(--e))` 驱动三层光效透明度，700ms 过渡把 1Hz 阶跃抹成呼吸。
- 三层：halo（球缘径向光环 inset:-5px + ::after 外晕；3.6s scale 呼吸——透明度归强度、呼吸只动 transform，两属性不打架）、core（球心 38% 径向光斑）、sheen（30° conic 高光叶 7s 旋转，静默时唯一动的东西）。
- 状态语义：暂停定格 --e + 动画放慢；断连/首帧前 is-offline（动画全停、0.22 轮廓）；prefers-reduced-motion 停装饰动画。
- 全部 color-mix 出自 --down/--up，主题换色光效跟走；动画只有 transform/opacity，合成器直出。
- 验证：深色底截图四状态 + 机读断言 --e = 1/0/1(定格)/0。**坑：Chrome --default-background-color 是 RGBA 顺序**，写 ARGB 得到纯红页面。

## 能量球粒子层（用户指令：上网找开源的用）

- 调研 5 个候选，采用 **MIT 的 santscoder-labs/project-13-energy-ball**（canvas 轨道粒子 + shadowBlur 发光），Three.js/GLSL 类对 92px 常驻窗太重，WinternetMeter/NetSpeed-Dynamic 是整个应用不是组件。
- 移植改造：14 粒子（双向旋转 + 正弦游动）走内外环之间带里（r 27~39），转速/亮度/光晕/粒径全随 eSmooth（1Hz 目标 0.06/帧插值）；颜色取 --down 计算值；paused 定格画布、offline 清空、reduced-motion 单帧、面板展开空转 + wake() 重启；DPR 感知。
- **headless 大坑**：`--virtual-time-budget` 下 rAF 不触发（canvas 恒空白，toDataURL 658 字符），必须加 `--run-all-compositor-stages-before-draw`；CSS transition 同样冻结在起始值（截图光核偏暗是假象，真实环境 0.86）。探针注入复用同一 preview.html 时注意锚点已被上一个探针吃掉。

## Sniffnet 调研（用户指令：看有哪些可以拿过来用）

- 调研 https://github.com/GyulyVGC/sniffnet（v1.5.1，MIT/Apache-2.0，Rust + iced GUI + pcap 抓包）。
- 核心结论：**数据面引擎不可移植也不该移植**——sniffnet 走 pcap 原始包（需 Npcap 驱动+管理员），NetPeek 走 ETW 天然有进程归因、免驱动，是 NetPeek 的优势。
- 可借鉴排序：①services.txt 端口服务映射（12093 条，build.rs 编译期生成 phf Map<(port,protocol),Service>，可在 NetPeek 前端/C# 精简嵌入）；②嵌入 MMDB 离线 IP→国家/ASN（maxminddb crate + DB-IP CC BY 4.0，需署名）；③阈值通知系统（notifications 模块：包/字节/设备/收藏主机 → toast+mp3+远程 webhook）；④check_updates.rs（GitHub Releases + semver）；⑤工程实践：workspace lints（clippy::pedantic + forbid unsafe）、SignPath 免费签名、WiX setup.wxs。
- 不可拿：packet-parser crate、plotters-iced2 图表（iced 生态）、listeners crate（ETW 不需要）、rodio/mp3。
- 用户尚未拍板做哪几项。

## Sniffnet 借鉴 1-4 落地（用户指令「先实现1-4」）

四项全部实现，均未 commit：

- **①端口服务名**：新增 `ui/services.js`（`NetPeekServices.serviceName/flagOf/peerLabel`，IANA 常用端口精简表 + 区域旗帜 emoji）。
- **③国家列**：下载 DB-IP Country Lite（CC BY 4.0，2026-09 版，8.3 MB）到 `src-tauri/resources/`，`geo.rs` 用 maxminddb crate 离线查（`country_code(ip)`，私有函数 `set` 注入便于单测）；pipe.rs 每帧 `augment_countries` 补 `TopRemoteCountry`。
- **采集端新增「对端」维度**（前置工程）：ETW 事件本就带远端 IP/端口，此前未采集。`EtwSnapshotSource.TrackEndpoint` 做「窗口内按 IP 累计字节、取榜首」——字典仅 ETW 回调线程访问，`TopIp/TopPort/WindowId` 跨线程走 Volatile/Interlocked，快照线程读完推进一元代数触发重计（读写各忍半帧偏差）。注意 `data.daddr` 是 **IPAddress 对象不是字符串**；IPv6 UDP 类名被库拼成 `UpdIpV6TraceData`（少个 d）。
- **②阈值通知**：`tauri-plugin-notification` + capabilities 放行；设置页加 downAlertMb/upAlertMb；pipe.rs 逐帧 `check_rate_alerts`，5 分钟冷却防刷屏。
- **④lints**：Cargo.toml `[lints.rust] unsafe_code="forbid"` 等；clippy 存量 92 条告警（unwrap_used 14 条是旧债），暂不阻断构建。
- **前端**：进程表加「对端」列（旗帜+IP+服务名）、详情面板与聚合视图同步、搜索覆盖对端、关于页加 DB-IP 署名。
- **踩坑（重要）**：同一文件的多个 Edit **并行下发会互相覆盖**——main.js/index.html 各有 3-4 处编辑静默丢失（buildRow 重写被冲、`_memberBytes:-1` 初始化丢失导致聚合视图恒显「—」）。**同一文件必须串行改，改完 grep 核对落盘**。
- 验证：dotnet build 0 警 0 错、cargo check + `cargo test geo::`（真实库查 8.8.8.8→US）、node --check 全过、headless Chrome 新增 peer-live/settings-alerts 两用例断言全过，截图 .workbuddy/verify/2026-09-11-peer-live.png。

## Sniffnet 第二轮深挖（用户指令：继续深入看还有哪些可学）

按 `contents` API 逐目录下钻（`git/trees?recursive=1` 响应过大必被截断）。这轮挖到的是**工程骨架层**，比第一轮的功能模块更值钱：

- **运行时可靠性（最高价值）**：①`error_logger.rs` 的 `ErrorLogger` trait + `location!()` 宏——错误统一打成 `文件:行号: 消息`，**debug 下直接 panic**（强制暴露），release 用 `gag` 把 stdout/stderr 重定向进 logs.txt，启动 truncate、`--logs` 用 explorer 打开；②`take_but_leave_something()`——取走累计值但**留下 last_timestamp/dropped_packets**，避免 tick 间隙数据丢；③配置容错三件套（`#[serde(default)]` + 字段级 `deserialize_with="deserialize_or_default"` + 加载后 `sanitize()` 修正越界值）。
- **网络语义**：`bogon.rs` 23 组 IPv4/IPv6 保留网段表（含 100.64/10 CGNAT、198.18/15 benchmark、2001:db8::/32）可直接翻成 JS，用于给「对端」列标「局域网」；`capture.rs` 的 **rDNS 5 线程池 + addresses_waiting_resolution 去重 + addresses_resolved 缓存**（阻塞查询不拖慢别人、同一 IP 不重复查）；`get_domain_from_r_dns` 的 eTLD+1 提取（带 30+ 条边界单测）。
- **数据呈现**：`data_representation.rs` 用 1000 进制（与 NetPeek 一致）但有两条 NetPeek 没有的规则——**n>999 强制截断防跨单位进位**、**仅当单位≠B 且 n≤9.95 时保留 1 位小数**；`DataRepr` 三态（Bytes/Bits/Packets）+ `ByteMultiple` 自动选单位。
- **交互**：分页（每页 30）+ 搜索 + 三类收藏（Host/Service/Program，HashSet + 漏斗筛选按钮）+ 聚合统计，`get_searched_entries` 一次返回三元组；迷你窗 = donut + 折线 + 收藏 host/service 各 4 条（空态画脉冲点）。
- **工程**：clap 命令行（`--adapter`/`--logs`/`--config-path`/`--restore-default`，全部 `exclusive`，用 `Task::done().chain()` 把 CLI 意图注入 GUI 状态机）；7 个内置主题 + **外部 TOML 主题可加载**；翻译分片 translations.rs(134KB)/_2.._6；每个 utils 函数配极端值单测（含 u128::MAX）。
- **NetPeek 现状核对（据此判断缺口）**：历史屏**已有 CSV 导出**（history-ui.js exportCsv）；fmtRate/fmtBytes 用 1000 进制但**无 999 截断**；**采集端零日志落盘**（CollectorService 只有 2 处 catch：OperationCanceled/IOException，无全局钩子）；**无私网/局域网判定**；无分页与收藏。
- **顺手收尾**：Cargo.toml `opt-level` 停在对照实验的 `3`，已改回 `"s"`（注释补上实测数据与未测完说明）。

## 按建议顺序落地 1~4（用户指令「按顺序施工」）

四项全部完成并验证，细节见 `docs/开发进度.md` §17。

### 1. 采集端日志体系（新增）
- **根因**：服务跑 LocalSystem，Host 默认 provider（Console/EventSource/Debug）在服务模式下无处可去 = 零日志；且 `CollectorService` 只 catch IOException，其他异常触发 .NET 默认 `StopHost` 直接停进程 —— 「界面永远离线」查不出原因。
- 新增 `src/NetPeek.Collector/Logging/CollectorLog.cs`：`%ProgramData%\NetPeek\collector.log`，2MB 轮转保留 2 份，写失败静默（日志不得成为故障源）；同文件 `FileLoggerProvider` 桥接现有 ILogger，采集端调用一处没改。
- `Program.cs`：`AppDomain.UnhandledException` + `TaskScheduler.UnobservedTaskException`（必须 `SetObserved()`）+ 启动横幅。
- `CollectorService.cs` 补通用 catch；`EtwSnapshotSource.cs` 事件线程由 `ProcessEvents()` 包装（原先裸调 `Source.Process()`，线程级异常=崩进程）。
- Rust 加 `collector_log_path` / `open_collector_log`；设置页「采集服务」列加「查看日志」按钮（explorer `/select`）。

### 2. settings sanitize（参考 Sniffnet conf.rs）
`rateUnit` 白名单、`retentionDays` 限 `[0,3650]`（**0=永久保留必须放行**）、阈值非有限/负/超限归零、布尔类型校验；非法值退回默认而非钳边界。收口在 `merged_with_defaults`，init/load/save 三条路径共享。7 个单测。

### 3. bogon 判定（参考 Sniffnet bogon.rs）
`services.js` 加 `bogonLabel(ip)`：15 段 IPv4 区间 + IPv6 前缀表（ULA/链路本地/Teredo/6to4/NAT64/IPv4 映射）。IPv6 走「展开 32 位 hex 比前缀」。对端列显示 `局域网 192.168.1.50 · SMB`，公网才挂国旗。

### 4. 格式化截断 + 顺带发现
`clampUnit()` 修 `999_999 → "1000.0 KB"` 跨单位。**写单测时发现 `fmtRate` auto 模式根本没有 GB 档**（fmtBytes 有），万兆链路会被压在 999.00 MB/s —— 一并补上。

### 新增：UI 单测（为此而生）
`scripts/ui-tests/`：`_harness.mjs` 用大括号配平从 `ui/*.js` 抽函数（浏览器 IIFE 不能直接 import）+ `new Function` 参数化 + vm 加载；`services.test.mjs`(72 断言) / `format.test.mjs`(31 断言)；`node scripts/ui-tests/run-all.mjs` 一键跑。

### ⚠️ 环境坑：本机 headless Chrome 已失效（新增，重要）
- Chrome 升到 **152.0.7977.83**，`--headless --dump-dom` 连 `data:` URL 都零输出，stderr：`GPU process exited unexpectedly: exit_code=-1073741819`(0xC0000005) + `Network service crashed`。chrome-for-testing 152.0.7977.64 同样。
- 症状迷惑点：命令不报错、不退出，**静默挂死**（前台超时后被后台化，反复卡 3~4 分钟）。
- 附带坑：`chrome.exe --version` 在 Windows 上不输出，反而会**启动完整 GUI 浏览器**（日志里能看到加载 chrome://newtab）。查版本要看 `C:\Program Files\Google\Chrome\Application\` 下的版本号目录。
- **结论**：旧的 `%TEMP%\netpeek-preview` headless 验证链路暂时作废，改用 node 单测 + Rust 单测 + C# 实跑。

### 踩坑记录（本轮新增）
- **拼接漏分隔符**：`::ffff:` + `c0a8` + `0101` 少了中间的 `:`，拼出 8 位一组的假 IPv6 段，被分组校验判非法静默返回 null。定位靠"抽源码 + 逐步插桩"，不要靠读代码猜。
- Rust 编辑手滑把 `pub fn get_autostart()` 的签名删了（new_string 比 old_string 少内容）——**编辑后要回读确认**。
- Rust 中文字符串做函数名会触发 non_snake_case，单测函数名保持 ASCII。
