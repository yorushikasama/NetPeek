# NetPeek Design System

## 1. Visual Theme

NetPeek 采用冷静、精密、克制的桌面仪表风格。整体以深色半透明浮岛、柔和环境光和清晰的数据层级构成，营造通透但不喧闹的技术感。

视觉目标：

- 数据始终是画面主角
- 玻璃材质用于分层，不用于炫技
- 界面像原生 Windows 桌面工具，而非传统管理后台
- 背景图缺失、服务断开、无数据及窄窗口时仍保持完整可用
- 动效只表达采集、选择、切换和反馈状态

## 2. Color Palette

### Foundation

- `--bg-deep`: 接近黑色的深蓝灰，用于无底图状态和最底层背景
- `--surface-primary`: 主数据浮岛的深色半透明表面
- `--surface-secondary`: 导航、检查栏和辅助浮岛表面
- `--surface-hover`: 交互项悬停时的轻微提亮
- `--stroke-subtle`: 低对比度边界线
- `--stroke-active`: 当前选择或聚焦状态边界

### Text

- `--text-primary`: 主要数值、标题和关键��状态
- `--text-secondary`: 标签、单位和辅助说明
- `--text-muted`: 次要元数据与不可用状态
- `--text-on-accent`: 强调色表面上的高对比文字

### Semantic Accents

- 下载使用冷青蓝体系
- 上传使用暖紫或洋红体系
- 成功与服务在线使用克制的青绿色
- 警告使用琥珀色
- 错误与服务断开使用柔和红色

强调色应主要出现在图表、关键数值、选中状态和状态指示器中。避免大面积高饱和填充、霓�虹泛光和多种强调色同时竞争。

## 3. Typography

### Roles

- 界面正文、导航、按钮和说明使用清晰的无衬线 UI 字体
- 实时速率、累计流量、图表刻度和其他关键数字使用等宽或数字宽度稳定的字体
- 标题通过字重、尺寸和留白建立层级，不依赖全大写或过度字距

### Hierarchy

1. Hero 数值：实时上传与下载速率
2. 页面标题及关键汇总
3. 区域标题和应用名称
4. 标签、单位、日期及�辅助信息
5. 注释、统计口径和不可用提示

数字与单位必须在视觉上形成一个整体，但单位权重低于数值。任何尺寸下都不得隐藏关键单位。

## 4. Layout

### Main Shell

主窗口由三个职责明确的岛屿组成：

1. 左侧导航岛，用于页面切换和应用级入口
2. 中央数据岛，用于当前页面的核心任务
3. 右侧检查岛，用于选中对象详情、统计口径和辅助操作

岛屿之间使用稳定间距分隔。�不要通过密集边框、嵌套卡片或大量阴影制造层级。

### Responsive Behavior

- 宽屏使用导航、主数据区和检查区三列布局
- 中等宽度优先保留主数据区，并将检查区调整为较窄列或下方区域
- 更窄窗口采用单列重排，而不是直接隐藏主内容
- 低高度窗口中，只允许长列表或明确的内容区域内部滚动
- 历史图表必须拥有稳定高度，避免随数据或窗口变化塌缩
- 窗口尺寸与设备像素比变�化后，Canvas 图表需要安全重绘
- 最低可用布局目标为 940 × 620

### Spacing

采用有限且重复使用的间距尺度。紧密间距用于标签与数值，常规间距用于组件内部，较大间距用于浮岛和任务区域之间。避免随意出现只使用一次的间距值。

## 5. Surfaces and Glass

- 主数据岛拥有最清晰、最稳定的表面和最高内容对比度
- 导航岛与检查岛可以更轻、更透明，但不能牺牲文字可读性
- 背景模�糊应适度，并提供无模糊或无背景图时的可靠实色退化
- 边框保持低对比度，仅用于界定浮岛、聚焦和选中状态
- 阴影应柔和且范围有限，不制造悬浮网页卡片感
- 辉光只围绕当前数据、选中柱或在线状态局部使用

## 6. Components

### Navigation

- 使用图标与可理解的标签或提示共同表达目的
- 当前页面通过位置、表面和强调色共同标识
- 不依赖悬停才能理解关键入口
- 窄布局下保�持完整导航能力

### Metric Blocks

- 大数字优先，标签与单位作为辅助
- 上传、下载颜色必须在全产品中保持一致
- 数值更新时避免造成布局抖动
- 无数据时显示明确占位状态，不使用伪造数字

### Charts

- 图表优先帮助用户比较趋势、峰值和应用差异
- 网格线与坐标轴保持低对比度
- 选中柱使用清晰顶帽、强调边缘或局部亮度，不仅依赖颜色变化
- 选中状态必须同步更新检查栏�
- Canvas 在尺寸和 DPR 变化后重绘，并保证文字与线条清晰
- 空数据、加载中和服务断开状态必须独立设计

### Application Lists

- 应用图标、名称、上传、下载与累计信息建立稳定列关系
- Top 8 或其他排行列表在低高度下仅列表本身滚动
- 当前选中项需同时使用表面、边界或标记表达
- 进程身份不确定时明确标示，不制造错误归因

### Forms and Date Controls

- 日期选择、自定义区间、按钮与输入框沿用浮岛系统的圆角、边界和焦点样式
- 标签始终可见，不能只依赖 placeholder
- 键盘焦点必须清晰
- 错误、空区间和无结果状态使用就地反馈

### Buttons

- 主操作使用有限强调色
- 次级操作使用中性半透明表面
- 危险操作必须使用独立语义色和明确文案
- 悬停、按下、键盘聚焦和禁用状态均需可辨识

### Empty and Disconnected States

- 无历史数据时解释原因并提供可执行��下一步
- 采集服务断开时明确展示状态、影响范围和恢复入口
- 无背景图时使用经过设计的深色环境背景，而不是失去层级
- 空态插画或装饰不得超过状态信息和操作按钮的视觉权重

## 7. Motion

- 页面切换使用短促淡入或轻微位移
- 数据刷新尽量平滑，但不得延迟真实状态
- 选中柱、列表项和检查栏之间可使用一致的状态过渡
- 尊重系统减少动态效果设置
- 禁止持续漂浮、无意��义闪烁、大范围呼吸光和影响数字读取的动画

## 8. Accessibility

- 正文、关键数值、按钮和状态文字保持充足对比度
- 上传与下载不得只依赖颜色区分，需结合标签、图标或位置
- 所有交互组件支持键盘操作和可见焦点
- 图标按钮提供可访问名称
- 状态变化通过可理解文字表达
- 高 DPI 下保持文字、图标和 Canvas 清晰

## 9. Screen-Specific Guidance

### Realtime

首先回答“现在谁在使用��网络”。实时上传、下载、趋势和活跃应用占据最高视觉优先级，进程详情与统计口径放在检查岛中渐进披露。

### History

首先回答“这段时间流量如何分布，谁使用最多”。日期范围、汇总、柱图和 Top 8 必须形成连续阅读路径。选中柱与右侧检查信息即时联动。

### Appearance

预览应真实反映当前背景、主题和玻璃参数。配置控件不可抢夺预览主体，且每项调整应有明确反馈。

### Settings

设置按采集、存储、启动行为和应用行为分组。危险或不可逆操作与常规偏好分离，并清楚说明影响。

### Desktop Widget

桌面小窗保持透明、置顶、不可调整尺寸和低干扰。只呈现最关键的实时信息，确保任何桌面背景上都可读。

## 10. Anti-Patterns

禁止以下实现：

- 在中等或窄宽度下直接隐藏中央主舞台
- 每个信息块都包裹成独立卡片
- 过度模糊、发光或高饱和渐��变
- 用装饰背景降低图表和正文对比度
- 使用内联样式承担长期布局职责
- 关键内容依赖 hover 才能出现
- 无单位、无状态说明或伪造精度的数据展示
- 为适配低高度而滚动整个应用外壳
- 视觉改动破坏采集逻辑、历史数据或 IPC 协议

## 11. Acceptance Checklist

- 1180 × 720 下三岛层级清晰，主要数据无需滚动即可理解
- 980px 附近主数据区不会消失，布局能够合理重排
- 更窄窗口采用可用的单列结构
- 低高度下只有必要的列表区域内部滚动
- 历史图表高度稳定，缩放及 DPR 变化后保持清晰
- 选中历史柱拥有明确顶帽或等效视觉反馈，并同步检查栏
- 自定义日期表单与现有浮岛风格一致
- 服务断开、无数据和无背景图状态完整可读
- 键盘焦点、对比度和状态表达满足桌面应用可访问性要求
- 不修改或破坏现有采集、存储和通信逻辑
