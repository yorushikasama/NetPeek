# NetPeek Product Context

## Product

NetPeek 是一款轻量、隐私优先的 Windows 桌面网络流量监控工具。它实时展示每个应用的上传、下载、累计流量与重传情况，并提供本地历史记录、应用排行、托盘常驻和桌面小窗。

## Target Users

- 希望快速了解各应用网络占用的 Windows 用户
- 排查后台程序、异常流量与网络性能问题的开发者和高级用户
- 需要长期观察流量趋势，但不希望数据�上传云端的隐私敏感用户

## Core Jobs

1. 一眼看清当前哪些应用正在使用网络，以及上传和下载速率。
2. 回看指定日期或区间的总流量、每日变化和应用排行。
3. 发现异常吞吐、重传或后台流量，并定位到具体进程。
4. 通过托盘与桌面小窗长期低干扰地观察网络状态。
5. 在不牺牲可读性的前提下，让界面适配用户选定的背景与配色。

## Product Principles

- **信息优先**：速率、累��计量、应用身份和异常状态必须比装饰更醒目。
- **低干扰常驻**：空闲时安静，需要时能迅速展开细节。
- **本地与可信**：采集、聚合和历史数据默认留在本机。
- **真实边界**：明确说明 ETW 统计口径、进程归因限制和服务连接状态，不制造虚假精度。
- **渐进披露**：主界面先回答“现在发生了什么”，检查栏和历史页再提供深入信息。
- **稳健胜过炫技**：视觉效果不能损害低端设备性能、窄窗口布局或数据可读性。

## Brand Personality

NetPeek 应当显得冷静、精密、克制且略带未来感，像一台设计精良的桌面网络仪表，而不是企业后台、游戏启动器或夸张的科幻控制台。

关键词：

- 精密
- 安静
- 通透
- 可信
- 有温度的技术感
- 桌面原生感

## Anti-References

避免以下方向：

- 卡片密集、留白不足的传统管理后台
- 霓虹泛滥、文字难读的赛博朋克界�面
- 所有区域都使用相同权重和相同玻璃材质
- 大面积纯装饰图形抢夺数据注意力
- 依赖悬停才能理解的关键功能
- 为追求极简而隐藏单位、状态或统计口径
- 窄窗口直接隐藏主要内容，而不是进行合理重排

## Strategic Design Principles

### 1. 岛屿式信息层级

导航、主数据区和检查区是职责明确的独立岛屿。岛屿通过位置、尺寸和材质建立层级，而不是堆叠大量边框和卡片。

### 2. 数据是视觉主角

实时速率、趋势、历史柱图与应用排行占据主要视觉预算。背景、辉光和玻璃效果只用于提供氛围与分层。

### 3. 一屏一个核心问题

- 实时页：现在谁在使用网络？
- 历史页：这段时间流量如何分布，谁使用最多？
- 外观页：怎样让界面与桌面环境协调？
- 设置页：采集、存储和应用行为如何配置？

### 4. 明确处理退化场景

无背景图、服务断开、无历史数��据、低高度、中等宽度、窄窗口和高 DPI 都必须拥有经过设计的稳定状态，不能简单隐藏核心内容。

### 5. 动效表达状态

动效用于传达采集活跃、页面切换、选择和反馈。避免持续漂浮、无意义脉冲或影响数值读取的动画。

## Product Constraints

- 仅面向 Windows 桌面环境。
- UI 使用 Tauri 2，并以普通用户权限运行。
- ETW 网络采集由独立的 .NET 8 LocalSystem 服务承担。
- 应用与采集服务通过 Named Pipe 通信。
- 历史数据使用本地 SQLite，默认保留 30 天。
- 主窗口必须支持至少 940 × 620 的可用布局。
- 桌面小窗为独立、透明、置顶且不可调整尺寸的窗口。
- 视觉优化不得破坏现有采集逻辑、历史数据或 IPC 协议。
