## 实施计划

1. **补齐历史页布局与无数据/筛选状态**
   - 调整 `src/NetPeek.App/ui/index.html` 的历史页结构，使工具栏、日期范围、统计汇总、图表与排行区域按既定设计落地。
   - 保留现有 DOM ID 和数据绑定接口，避免破坏现有事件逻辑。
   - 清理本次历史页范围内可由 CSS 类替代的内联样式。

2. **完善历史页交互逻辑**
   - 在 `src/NetPeek.App/ui/history-ui.js` 中完善预设时间范围与自��定义日期范围切换。
   - 加入非法日期、起止日期倒置等校验，并正确展示和清除错误状态。
   - 确保时间范围变化后，汇总数据、趋势图和排行列表同步刷新。
   - 保留当前通过 CSS 自定义属性设置排行占比的实现方式。

3. **补齐图表选中态与标签表现**
   - 复用 `src/NetPeek.App/ui/charts.js` 现有图表 API，不另建重复绘图库。
   - 核对柱状图选中索引、顶部强调线、刻度标签和紧凑数值格式，补齐历史页调用侧尚未连接的参数或状态。
   - 处理空数据、单点数据和窄尺寸下的显示边界。

4. **完成视觉样式与响应式适配**
   - 在 `src/NetPeek.App/ui/styles.css` 中补齐历史工具栏、日期字段、错误提示、汇总卡片、图表、排行进度背景和滚动区域样式。
   - 与现有浮岛版视觉变量保持一致，避免引入新的平行设计体系。
   - 完善 1080px 和 980px 附近的响应式布局，防止筛�选控件、图表和排行区域溢出或被错误隐藏。

5. **验证并控制修改范围**
   - 运行项目已有的前端检查或测试，并执行针对历史页脚本的语法检查。
   - 检查最终 diff，确认只修改历史页落地所需文件。
   - 不触碰当前已有的 `src/NetPeek.App/src-tauri/Cargo.toml` 修改和未跟踪的 `icon-d1tools.com.ico`，也不覆盖用户现有工作。
   - 完成后汇报改动文件、实现内容和验证结果；不在本轮擅自提交�。